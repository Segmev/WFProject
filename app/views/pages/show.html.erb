<div class="row" class="center_image">
  <h1>
    <%= @page.title %>
  </h1>
  <p class="desc">
    <%= @page.content %>
  </p>
  <%= render partial: "pictures", collection: @page.pictures %>
</div>
<br>
<%= link_to 'Edit', edit_page_path(@page), :class => 'btn btn-primary' %>
<%= link_to 'Back', page_path, :class => 'btn btn-primary' %>
