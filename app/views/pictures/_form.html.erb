<%= form_with(model: picture, local: true) do |form| %>
  <% if picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
      <% picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :alt_text %>
    <%= form.text_field :alt_text, id: :picture_alt_text %>
  </div>

  <div class="field">
    <%= form.label :caption %>
    <%= form.text_field :caption, id: :picture_caption %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :picture_image %>
  </div>

  <div class="field">
    <%= form.label :page_id %>
    <%= form.number_field :page_id, id: :picture_page_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
